// Diesen Block in Firebase → Firestore → Regeln einfügen
// Position: NACH dem bestehenden match /fahrer/... Block, VOR den beiden letzten }
//
// Projekt: collectionshop-2854d
// Editor: https://console.firebase.google.com/project/collectionshop-2854d/firestore/rules
//
// Danach: Veröffentlichen → in der Fahrer-App neu einloggen.
// Ermöglicht: Login (read) + Online-Toggle (write) für das eigene user-Dokument.

    match /user/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
